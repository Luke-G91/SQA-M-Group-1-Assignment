extends ../layout

block content
  h2 Write a Comment
  form(action=`/blog/${post.id}/comment` method="post")
    textarea(name="comment", placeholder="Write a comment...", maxlength="500")
    p 500 word limit
    button(type="submit") Post Comment
    p.error-message(style="display: none;")
